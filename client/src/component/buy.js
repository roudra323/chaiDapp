import React from "react";

const buy = ({ state }) => {
  const buyChai = async () => {};

  return (
    <React.Fragment>
      <form>
        <button onClick={buyChai}>Buy Chai</button>
      </form>
    </React.Fragment>
  );
};

export default buy;
